import { MongoDBAdapter } from "./mongodb.adapter";

describe("MongoDBAdapter", () => {
	test("should be defined", () => {
		expect(MongoDBAdapter).toBeDefined();
	});

	// Add more tests for the MongoDBAdapter class here
});
